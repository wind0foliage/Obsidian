Vue 开发组件：
~~~
<FiMselect name="packageId" label="耗材品" transform="@ConsumeSpecAll"/>
~~~
FiMselect 组件
下拉数据来源：
	transform > callback > url
下拉框没有提供隐藏能力


存储过程临时变量：
~~~mysql
IF (V_TransferType IN (0, 1)) THEN  
            -- 暂存区-质检区  
            IF (EXISTS(SElECT 1 FROM tmp_xchg WHERE f3 = 0  
            AND (@tmp_query := f2) IS NOT NULL  
            AND EXISTS (  
                SELECT 1 FROM cfg_warehouse_position cwp  
                JOIN cfg_warehouse_zone cwz ON cwz.zone_id = cwp.zone_id  
                WHERE cwp.rec_id = FN_GET_INT('position_out_id', 0)  
                  AND cwz.is_pop = 6  
                  AND cwz.type = 1  
            ))) THEN  
                SET @sys_code = 21;  
                SET @sys_message = CONCAT("暂存质检区货位需要进行质检转正/转残操作，无法直接操作。");  
                LEAVE MAIN_LABEL;  
            END IF;  
        END IF;
        
set @tmp_query = P_Query;
~~~
@tmp_query := f2
和
@tmp_query = P_Query;

现象： 无法取到正确值, 第一个f2无法取到正确的值
原因：没找到
规避方法： 使用游标代替










下标签：处的搜索条件展示，无法支持多个tab页的显示


![[Pasted image 20260211160427.png]]

Htmlayout.Element.ToQueryParam

btnFirstPage 获取方式写死
Element btnFirstPage = null;  
ForEach(delegate(Element x)  
{  
    if (btnFirstPage == null)  
    {        btnFirstPage = x.SelectParent("body").Q("#btn_first_page");  
        if (btnFirstPage == null)  
        {btnFirstPage = x.SelectParent("body").Q("#btn_first_page");  
        }  
    }
 }
![[Pasted image 20260211160430.png]]

table 和 底部栏绑定 - 不能使用一个底部栏，一个底部栏只能绑定一个table

![[Pasted image 20260211160434.png]]

解决方案，增加新的框架适配代码
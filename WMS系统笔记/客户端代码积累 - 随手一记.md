表头字段转换类 ：FieldTransform.cpp

慢sql 查询 - 查看当前运行的sql：
SELECT NOW(), a.* FROM information_schema.processlist a WHERE Command != 'Sleep' ORDER BY Time ASC;

系统配置表：sys_setting

static SQInteger _element_xcall(HSQUIRRELVM v)  xcall 函数，核心方法  
htmlayout_dom.hpp 核心文件
sq_dom.cpp 核心文件
db_behavior.hpp 行为定义文件
new_data_import_behavior.hpp 导入、导出行为

获取系统配置
CALL SP_UTILS_GET_CFG('key', V_ShipWeightRoundUp, 0);

I_SALES_DELIVER_TRADE 递交订单

开发模板存储过程：SP_CFG_LOGISTICS_SENDER_UPDATE

异常抛出报错
CALL SP_ASSERT(V_Mode<>2, '该参数不可修改'); 

通用权限表：dict_rights

导入功能核心存储过程：SP_IMPORT_COMMIT

日志在哪查：
系统日志：
sys_log
sys_log_other (operator_id): 其他系统日志
sys_login_account (account)：登录帐号，此表允许删除记录
sys_login_account_log(account_id) :登录帐号日志
sys_login_account_operation_log : 登录账号操作日志

耗材日志表：
consume_inout_log ： 耗材进出日志表
his_sales_trade_log：订单日志

超过时间的订单会被转移到其他地方



解密获取获取值函数
FN_ENCRYPTION_LEVEL1(FN_FIELD_RIGHT_VALUE(st.receiver_address, 31))

客户端多权限或控制：一个组件可以同时被多个权限控制，只要有一个权限按钮即可显示
require="xxx,xxxx,xxx"


 完整特殊货位列表

| position_id | 货位名称  | 用途说明       | 使用场景          |
| ----------- | ----- | ---------- | ------------- |
| -1          | 未知货位  | 货位信息未知或未维护 | 出库单中货位未指定时使用  |
| -2          | 发货暂存位 | 发货前的临时存放   | 验货完成后、发货前的暂存  |
| -3          | 盘点暂存位 | 盘点过程中的临时存放 | 盘点时从原货位移出的货品  |
| -4          | 移位暂存位 | 移位过程中的临时存放 | 移位单执行过程中的中转   |
| -5          | 补货暂存位 | 补货过程中的临时存放 | 从备货区补货到拣货区的中转 |
| -6          | 耗材发货位 | 耗材发货专用虚拟货位 | 耗材库存发货时使用     |

货位分配：
I_DISTRIBUTION_POSITION


数据库查看触发器命令：SHOW TRIGGERS ;
后台管理系统 ——> 客户信息 --------> 触发器查询





chg_history 保留时间 1.5年

SP_CLEAN_DATA这个存储过程是清理各类表的数据，里面备注了最长保留时间


查询授权码 - 自动化任务修改授权码过期时间
SELECT CONCAT(CHAR(10),  
'卖家账号', CHAR(10), CAST(FROM_BASE64(SUBSTRING_INDEX(auth_code,'~',1)) AS CHAR), CHAR(10),  
'账号：', account, CHAR(10),  
'密码：', CAST(COALESCE(AES_DECRYPT(FROM_BASE64(origin_password), '{AZ20uw9'),DECODE(FROM_BASE64(origin_password), '{AZ20uw9')) AS CHAR), CHAR(10),  
'授权码：', auth_code, CHAR(10),  
'已兑换次数：', use_count, CHAR(10),  
'过期时间：', expire_time, CHAR(10)  
) AS 授权信息  
FROM sys_login_auth_code  
WHERE expire_time > NOW() AND ((@指定授权码 := '') = '' OR auth_code = @指定授权码)\G

UPDATE sys_login_auth_code SET expire_time = '2025-12-24 20:12:33' WHERE rec_id = 93;

单链接
正常执行的超时时间是45s  4min是连接的持续时间



### 使用PowerShell查看（输出更清晰）

powershell

编辑

```
1Get-NetTCPConnection -LocalPort 端口号 | Select-Object LocalPort, OwningProcess
```



## 二、解除端口占用方法

### 1. 命令行终止进程（推荐）

cmd

编辑

```
1taskkill /pid PID号 /f /t
```



-- 端口转发  
netsh interface portproxy add v4tov4 listenaddress=127.0.0.1 listenport=30000 connectaddress=112.126.80.119 connectport=30002  
-- 取消端口转发  
netsh interface portproxy delete v4tov4 listenaddress=127.0.0.1 listenport=30000

数据库修改语句：
UPDATE hr_employee SET PASSWORD= FN_PBKDF2_SHA256(MD5('1'),salt,1000,32) WHERE employee_no='';


git 查看某一行的历史
git log -L 132000,132010:sp.sql --pretty=format:"%h - %an, %ar : %s"


```
SHOW CREATE PROCEDURE `SP_OWNER_FEE_QUERY`;
```


推送订单
SP_API_PUSH_TRADE

undo commit 上一次提交
git reset --soft HEAD^


网页端服务器：堡垒机 web
![[Pasted image 20260211160441.png]]![[Pasted image 20260211160445.png]]


订单归档：
SP_SALES_TRADE_AUTO_FILE  
SP_SALES_TRADE_LOG_AUTO_FILE


重复问题
订单审核200单
large_data_operation_batch_count


窦甲臣 2025/12/20 16:19:56  
MySQL 登录信息  
/usr/local/mysql/bin/mysql -A -uwdtreader  -h 172.17.31.151 -P40011 -p  
  
卖家账号 = kangdele  
用户 = wdtreader  
	IP = 172.17.31.151  
端口 = 40011  
密码 = WMS.99d9cf6b0b1eccd96b452979d6d45e8e  
登录wms_yace服务器之后跳转过去  
  
窦甲臣 2025/12/20 16:20:17  
kangdele是私有化部署的，和阿里云卖家不一样


I_TRADE_MATCH_LOGISTICS  
这个是物流匹配的代码

代码数据回传
SELECT GROUP_CONCAT(CONCAT(f1, '-', f2, '-', f3) SEPARATOR ',') INTO  @result FROM tmp_xchg;

CALL record_plog("SP_STOCKOUT_ORDER_UNSALES_BATCH_GET", @result);

insert into tmp_xchg_wms_test (f1,f2) values (stockout_id,V_SQL);
根据name查找货主id：
货主表： sys_goods_owner
select * from sys_goods_owner sgo where short_name = '甜水富文-云集回传';

常用字段：
owner_id;
short_name;

根据name/id查找仓库：
仓库表：sys_warehouse

select * from sys_warehouse sw where name like '%hwq仓库%';

常用字段：
warehouse_id
warehouse_no
name
select sgo.owner_id,sw.warehouse_id from sys_goods_owner sgo

根据货位名称找id；
cfg_warehouse_position

select * from cfg_warehouse_position where position_no = 'A-C';
select * from cfg_warehouse_position where position_no = 'SH002' and warehouse_id = 17;

select * from cfg_warehouse_position where rec_id = 12590;
spec_id = 126149

查看配置值
select * from sys_setting where `key` in ('multi_logistics_print_sequence_optimize');


select * from sys_setting where `key`  = 'warehouse_auto_task';


select * from sys_setting where `key`  = 'register_use_recommend_package_type'\G

# 查询当前员工id

SELECT count(DISTINCT eo.owner_id ORDER BY eo.owner_id)
		FROM sys_goods_owner o,cfg_employee_owner eo 
		WHERE eo.owner_id=o.owner_id AND eo.employee_id=277 AND eo.is_denied=0 AND o.is_disabled=0;
		
select *  from hr_employee limit 1;